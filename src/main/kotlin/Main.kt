import javax.annotation.processing.Messager
import kotlin.random.Random

/* Top Level Properties and Functions
 * These are any properties or functions defined outside of an
 * enclosing class
 */
fun main(): Unit {
    println(hoursInMins)
    print(millisForHours(12))
    print(millisForHoursForMdule(12))
}
